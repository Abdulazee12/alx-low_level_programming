#include <stdio.h>

/**
 * main - This program prints every letter of the alphabet
 * Return: 0 (Success)
 */

char main(void)
{
	char a;

	for (a = 97; a <= 122; a++)
	{
		if (a != 101 && a != 113)
			putchar(i);
	}
	putchar('\n');
	return (0);
}
